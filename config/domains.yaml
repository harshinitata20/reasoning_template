domains:
  # Add your domain-specific configurations here
  # Example:
  # domain_name:
  #   description: "Description of what this domain does"
  #   enabled: true
  example_domain:
    description: "Example mock domain used for testing the pipeline"
    enabled: true
  #
  # The domain-agnostic pipeline will route queries and tools based on these entries.
  # Domain-specific tool implementations go in tools/domains/domain_name/
